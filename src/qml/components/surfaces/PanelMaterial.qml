import QtQuick
import LVRS 1.0

MaterialSurface {
    density: MaterialSurface.Glass25
    radius: Theme.materialPanelRadius
    implicitWidth: Theme.scaleMetric(380)
    implicitHeight: Theme.scaleMetric(204)
}

// LV.PanelMaterial { anchors.fill: parent; primaryColor: LV.Theme.primary }
